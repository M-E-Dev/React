export const reducer = (state, action) => {
    switch (action.type) {
        case "FETCH_START":
        case "FETCH_SUCCESS":
        case "FETCH_FAIL":
            
            break;
    
        default:
            break;
    }
}